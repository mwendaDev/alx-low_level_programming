#include "main.h"
/**
*_islower - function to check for lowercase character
*@c: character to print
*Return: on success 1
*/
int _islower(int c);
{
int c = 99;
if (c == 99)
return (1);
else
return (0);
}
