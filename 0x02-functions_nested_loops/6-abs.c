#include "main.h"
/**
*_abs - function to find the absolute value
*@n: the absolute value to print
*Return: on success 1
*/
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (1);
}
