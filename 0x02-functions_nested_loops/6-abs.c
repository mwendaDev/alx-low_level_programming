#include "main.h"
/**
*_abs - function to find the absolute value
*@int: the absolute value to print
*Return: on success 1
*/
int _abs(int)
{
if (int < 0)
{
return (-int);
}
else
{
return (int);
}
return (1);
}
