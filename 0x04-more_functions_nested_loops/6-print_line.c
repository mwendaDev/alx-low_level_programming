/**
*print_line - func to ptint line
*@n: number of characters to print
*/
void print_line(int n)
{
int i;
int k = 95;
for (i = 1; i <= n; i++)
{
if ((n != 0) || (n > 0))
_putchar(k);
}
return (0);
}
