#include <stdio.h>
/**
*main - entry point
*Return: 0 if executed
*/
int main(void)
{
char ch;
ch = 'a';
do {
putchar(ch);
ch++;
} while (ch <= 'z' && (ch == 'q' || 'e');
putchar('\n');
return (0);
}
